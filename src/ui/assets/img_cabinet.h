#pragma once
#include "lvgl/lvgl.h"

/* Cabinet vitrine image — 224 × 206 px, RGB565.
 *
 * HOW TO REPLACE THIS STUB WITH THE REAL IMAGE:
 *   1. Resize the source photo to exactly 224 × 206 px.
 *   2. Open https://lvgl.io/tools/imageconverter
 *        Name          : img_cabinet
 *        Color format  : True color  (RGB565)
 *        Output format : C array
 *   3. Replace img_cabinet.c with the downloaded file.
 *      This header declaration stays the same — no other changes needed.
 */
LV_IMG_DECLARE(img_cabinet);
